import { CategorieListePipe } from './categorie-liste.pipe';

describe('CategorieListePipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieListePipe();
    expect(pipe).toBeTruthy();
  });
});
